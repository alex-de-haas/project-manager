name: Build and Publish Docker Image

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  packages: write

concurrency:
  group: docker-publish-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Normalize image name
        run: echo "IMAGE_NAME=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,format=short,prefix=sha-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Render Docker Host module metadata
        run: |
          node scripts/render-module-metadata.mjs \
            --repository "$IMAGE_NAME" \
            --tag "sha-${GITHUB_SHA::7}" \
            --output dist/module-metadata/metadata.json

      - name: Upload Docker Host module metadata
        uses: actions/upload-artifact@v4
        with:
          name: docker-host-module-metadata
          path: dist/module-metadata/metadata.json

      - name: Publish latest Docker Host module metadata release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          METADATA_RELEASE_TAG: latest
          METADATA_RELEASE_TITLE: Latest Docker Host module metadata
          METADATA_ASSET_PATH: dist/module-metadata/metadata.json
        run: |
          if gh release view "$METADATA_RELEASE_TAG" >/dev/null 2>&1; then
            gh release upload "$METADATA_RELEASE_TAG" "$METADATA_ASSET_PATH" --clobber
          else
            gh release create "$METADATA_RELEASE_TAG" "$METADATA_ASSET_PATH" \
              --target "$GITHUB_SHA" \
              --title "$METADATA_RELEASE_TITLE" \
              --notes "Mutable Docker Host metadata channel for the latest Project Manager image." \
              --latest=false
          fi
